{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build (return-the-day).c",
            "type": "shell",
            "command": "gcc ./${fileBasenameNoExtension}.c -o ./${fileBasenameNoExtension}.out",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}