{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build (return-the-day).c",
            "type": "shell",
            "command": "gcc ./return-the-day.c -o ./return-the-day.out",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": false
            }
        },
        {
            "label": "Build (make-uppercase).c",
            "type": "shell",
            "command": "gcc ./make-uppercase.c -o ./make-uppercase.out",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}